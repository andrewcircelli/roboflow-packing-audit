# Roboflow API key — used to authorize model/workflow pulls to the LOCAL server.
# The inference server at localhost:9001 uses this to fetch model weights once,
# then caches them. Copy to .env and fill in. .env is gitignored.
ROBOFLOW_API_KEY=
ROBOFLOW_WORKSPACE=
ROBOFLOW_WORKFLOW_ID=
